# vanity-id

Generate short, memorable IDs from names and text. Handles collisions automatically, avoids confusing characters (0/O, 1/I), and supports different entity types with appropriate length constraints.
